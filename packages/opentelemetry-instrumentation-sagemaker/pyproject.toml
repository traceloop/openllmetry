[project]
name = "opentelemetry-instrumentation-sagemaker"
version = "0.51.1"
description = "OpenTelemetry SageMaker instrumentation"
authors = [
  { name = "Bobby Lindsey", email = "bwlind@amazon.com" },
]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.10,<4"
dependencies = [
  "opentelemetry-api>=1.38.0,<2",
  "opentelemetry-instrumentation>=0.59b0",
  "opentelemetry-semantic-conventions-ai>=0.4.13,<0.5.0",
  "opentelemetry-semantic-conventions>=0.59b0",
]

[project.urls]
Repository = "https://github.com/traceloop/openllmetry/tree/main/packages/opentelemetry-instrumentation-sagemaker"

[project.optional-dependencies]
instruments = ["boto3"]

[project.entry-points."opentelemetry_instrumentor"]
sagemaker = "opentelemetry.instrumentation.sagemaker:SageMakerInstrumentor"

[dependency-groups]
dev = [
  "autopep8>=2.2.0,<3",
  "ruff>=0.4.0",
]
test = [
  "boto3>=1.34.120,<2",
  "opentelemetry-sdk>=1.38.0,<2",
  "pytest-recording>=0.13.1,<0.14.0",
  "pytest-sugar==1.0.0",
  "pytest>=8.2.2,<9",
  "vcrpy>=8.0.0,<9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["opentelemetry"]

[tool.coverage.run]
branch = true
source = ["opentelemetry/instrumentation/sagemaker"]

[tool.coverage.report]
exclude_lines = ["if TYPE_CHECKING:"]
show_missing = true

[tool.ruff]
line-length = 120
exclude = [
  ".git",
  "__pycache__",
  "build",
  "dist",
  ".venv",
  ".pytest_cache",
]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.uv]
constraint-dependencies = ["urllib3>=2.6.3", "pip>=25.3"]
